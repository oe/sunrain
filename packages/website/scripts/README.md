# 翻译管理脚本

这个目录包含用于管理和维护翻译文件的脚本工具。

## 脚本列表

### check-translations.ts

翻译完整性检测脚本，用于检查所有翻译文件的完整性，识别缺失的翻译键和未翻译的内容。

#### 功能特性

- ✅ 检测缺失的翻译键
- ✅ 识别可能未翻译的内容（与默认语言相同的文本）
- ✅ 检测缺失的翻译文件
- ✅ 生成详细的统计报告
- ✅ 支持按模块和语言分类的报告
- ✅ 输出JSON格式的详细报告

#### 使用方法

```bash
# 基本检查
npm run check-translations

# 生成详细报告到指定文件
npm run check-translations:report

# 或者直接使用tsx运行
tsx scripts/check-translations.ts

# 自定义输出文件
tsx scripts/check-translations.ts --output=my-report.json

# 查看帮助
tsx scripts/check-translations.ts --help
```

#### 输出说明

脚本会输出以下信息：

1. **总体统计**：模块数量、语言数量、问题总数、整体完成度
2. **按语言统计**：每种语言的翻译完成度和问题数量
3. **按模块统计**：每个模块在各语言中的翻译完成度
4. **问题详情**：
   - 缺失的文件
   - 缺失的翻译键
   - 可能未翻译的内容

#### 退出码

- `0`: 没有严重错误（可能有警告）
- `1`: 发现严重错误（缺失文件或缺失翻译键）

#### 配置

脚本中的配置常量：

```typescript
// 支持的语言列表
const SUPPORTED_LANGUAGES = ['en', 'zh', 'es', 'ja', 'ko', 'hi', 'ar'];

// 默认语言（作为基准）
const DEFAULT_LANGUAGE = 'en';

// 翻译模块列表
const TRANSLATION_MODULES = ['shared', 'home', 'guide', 'resources', 'about', 'assessment'];
```

#### 报告示例

```
📊 TRANSLATION COMPLETENESS REPORT
============================================================

📈 Overall Statistics:
  Total Modules: 6
  Total Languages: 7
  Total Issues: 33
  Overall Completeness: 99%

🌍 By Language:
  ✅ EN: 100% (368/368)
  ✅ ZH: 98% (362/368)
    Untranslated: 6 keys
  ✅ ES: 98% (361/368)
    Untranslated: 7 keys

📦 By Module:
  shared:
    ✅ zh: 97% (32/33)
    ✅ es: 97% (32/33)
  resources:
    ⚠️ zh: 88% (28/32)
    ⚠️ es: 84% (27/32)

❌ Issues Found:
  Possibly Untranslated (33):
    shared:
      zh: 1 items
      es: 1 items
    resources:
      zh: 4 items
      es: 5 items
```

## 开发说明

### 添加新脚本

1. 在此目录创建新的TypeScript文件
2. 在`package.json`中添加对应的npm脚本
3. 更新此README文档

### 依赖要求

- Node.js 18+
- tsx（用于运行TypeScript脚本）
- 项目的翻译文件结构

### 文件结构要求

脚本假设翻译文件按以下结构组织：

```
src/locales/
├── shared/
│   ├── en.ts
│   ├── zh.ts
│   └── ...
├── home/
│   ├── en.ts
│   ├── zh.ts
│   └── ...
└── ...
```

每个翻译文件应该导出一个默认对象或命名导出（如`sharedEn`、`sharedZh`等）。
